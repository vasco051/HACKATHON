export const staticLinks = {
  main: '/',
  authorization: '/authorization',
  registration: '/registration',
  category: '/categories/:category_id',
  courseBoard: '/categories/:category_id/courses/:course_id',

  // admin
  adminCategories: '/admin/categories',
  adminCategory: '/admin/categories/:category_id',
  adminCourse: '/admin/categories/:category_id/courses/:course_id'
};
